class CS:

    def __init__(self, full_path, cs_name, topic_class):
        self.full_path = full_path
        self.cs_name = cs_name
        self.topic = topic_class

        # Cheatsheet Metadata (not used)
        self.author = ""
        self.ref = []
        self.tags = []
